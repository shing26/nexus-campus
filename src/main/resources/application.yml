server:
  port: 8081
  servlet:
    context-path: /
    encoding:
      charset: UTF-8
      enabled: true
      force: true
  compression:
    enabled: true
    mime-types: application/json,application/xml,text/html,text/plain,text/css,application/javascript
    min-response-size: 1024

spring:
  application:
    name: nexus-vibe
  task:
    execution:
      pool:
        core-size: 4
        max-size: 10
        queue-capacity: 100
    thread-name-prefix: nexus-async-


  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.data.redis.RedisAutoConfiguration
      - org.springframework.boot.autoconfigure.data.redis.RedisRepositoriesAutoConfiguration

  sql:
    init:
      mode: always
      schema-locations: classpath:schema.sql
      data-locations: classpath:data.sql

  # JSP View Resolver

  # H2 Database (dev mode - switch to MySQL for production)
  datasource:
    url: jdbc:h2:mem:nexuscampus;DB_CLOSE_DELAY=-1;MODE=MYSQL;DATABASE_TO_LOWER=TRUE
    driver-class-name: org.h2.Driver
    username: sa
    password:

  h2:
    console:
      enabled: true
      path: /h2-console

  # Redis Configuration
  redis:
    enabled: true
    host: localhost
    port: 6379
    timeout: 3000
    lettuce:
      pool:
        max-active: 8
        max-idle: 8
        min-idle: 0

  # JPA / Hibernate
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true

  # File upload
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 20MB

# MyBatis-Plus
mybatis-plus:
  mapper-locations: classpath*:mapper/*.xml
  type-aliases-package: com.nexus.campus.entity
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    banner: false
    db-config:
      id-type: ASSIGN_ID
      logic-delete-field: deleted
      logic-delete-value: 1
      logic-not-delete-value: 0

# JWT Configuration
jwt:
  secret: OExHRjA3QjlGRjI0NUI0Q0E1RTdDRjI2MDA3QTUxMjhDOTdBNkY4RjI0NUI0Q0E1RTdDRjI2MDdCOTg2RjA3Qg==
  expiration: 86400000

# DFA Sensitive Words
campus:
  redis:
    enabled: false
  upload:
    dir: src/main/resources/static/uploads
  security:
    sensitive-words: fuck,shit,damn,asshole,bitch
    critical-words: 闂佸搫妫欏娆愭叏韫囨稑绀嗛柛鈩冨嚬濡?婵☆偅澹嗘慨楣冦€呴锕€鐐婇柦妯侯樈閸熲偓,闂備緡鍙庢禍婵嬪汲閳ь剛绱撴担绋款伀缂?闂佽鍘介崝鏍焵椤掍胶澧ч悗闈涙湰缁?闂佸搫顑勯懗鍫曨敂椤掑倵鍋撶憴鍕垫畷闁哄棌鍋?闂佸憡甯掑Λ婊埶夐崶鈺傚殜闁逞屽墰閹?闂佸憡鐟ョ粔瀛樻叏閳哄倻鈹嶉柍鈺佸暕缁?
    audit-required-level: 2

  ai:
    review:
      enabled: true
    llm:
      endpoint: https://api.openai.com/v1
      api-key: ${LLM_API_KEY}
      model: gpt-4o-mini
      timeout: 30s

# Logging
logging:
  level:
    com.nexus.campus: DEBUG
    org.springframework: INFO
    org.mybatis: INFO

---
management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: always

---
spring:
  config:
    activate:
      on-profile: mysql

  datasource:
    url: jdbc:mysql://localhost:3306/nexus_campus?useUnicode=true&characterEncoding=utf8mb4&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: root
    h2:
      console:
        enabled: false
    sql:
      init:
        mode: never

mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl