server:
  port: 8081
  servlet:
    context-path: /
    encoding:
      charset: UTF-8
      enabled: true
      force: true
  compression:
    enabled: true
    mime-types: application/json,application/xml,text/html,text/plain,text/css,application/javascript
    min-response-size: 1024

spring:
  application:
    name: nexus-vibe
  task:
    execution:
      pool:
        core-size: 4
        max-size: 10
        queue-capacity: 100
    thread-name-prefix: nexus-async-


  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.data.redis.RedisAutoConfiguration
      - org.springframework.boot.autoconfigure.data.redis.RedisRepositoriesAutoConfiguration

  sql:
    init:
      mode: always
      schema-locations: classpath:schema.sql
      data-locations: classpath:data.sql

  # JSP View Resolver

  # H2 Database (dev mode - switch to MySQL for production)
  datasource:
    url: jdbc:h2:mem:nexuscampus;DB_CLOSE_DELAY=-1;MODE=MYSQL;DATABASE_TO_LOWER=TRUE
    driver-class-name: org.h2.Driver
    username: sa
    password:

  h2:
    console:
      enabled: true
      path: /h2-console

  # Redis Configuration
  redis:
    enabled: true
    host: localhost
    port: 6379
    timeout: 3000
    lettuce:
      pool:
        max-active: 8
        max-idle: 8
        min-idle: 0

  # JPA / Hibernate
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true

  # File upload
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 20MB

# MyBatis-Plus
mybatis-plus:
  mapper-locations: classpath*:mapper/*.xml
  type-aliases-package: com.nexus.campus.entity
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    banner: false
    db-config:
      id-type: ASSIGN_ID
      logic-delete-field: deleted
      logic-delete-value: 1
      logic-not-delete-value: 0

# JWT Configuration
jwt:
  secret: OExHRjA3QjlGRjI0NUI0Q0E1RTdDRjI2MDA3QTUxMjhDOTdBNkY4RjI0NUI0Q0E1RTdDRjI2MDdCOTg2RjA3Qg==
  expiration: 86400000

# DFA Sensitive Words
campus:
  redis:
    enabled: false
  upload:
    dir: src/main/resources/static/uploads
  security:
    sensitive-words: fuck,shit,damn,asshole,bitch
    critical-words: 闂備礁鎼Λ娆忣焽濞嗘劖鍙忛煫鍥ㄧ☉缁€鍡涙煕閳╁啫鍤俊?濠碘槅鍋呮竟鍡樻叏妤ｅ啨鈧懘顢曢敃鈧悙濠囨煢濡警妯堥柛鐔插亾,闂傚倷绶￠崣搴㈢濠靛姹查柍褜鍓涚槐鎾存媴缁嬫浼€缂?闂備浇顕栭崢浠嬪礉閺嶎厼鐒垫い鎺嶈兌婢а囨倵闂堟稒婀扮紒?闂備礁鎼鍕嚄閸洦鏁傛い鎺戝€甸崑鎾舵喆閸曞灚鐣烽梺鍝勬閸?闂備礁鎲＄敮鎺懳涘鍩跺宕堕埡鍌氭疁闂侀€炲苯澧伴柟?闂備礁鎲￠悷銉х矓鐎涙ɑ鍙忛柍鍝勫€婚埞宥夋煃閳轰礁鏆曠紒?
    audit-required-level: 2

  ai:
    review:
      enabled: true
    llm:
      endpoint: https://api.openai.com/v1
      api-key: ${LLM_API_KEY:}
      model: gpt-4o
      timeout: 30s

# Logging
logging:
  level:
    com.nexus.campus: DEBUG
    org.springframework: INFO
    org.mybatis: INFO

---
management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: always

---
spring:
  config:
    activate:
      on-profile: mysql

  datasource:
    url: jdbc:mysql://localhost:3306/nexus_campus?useUnicode=true&characterEncoding=utf8mb4&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: root
    h2:
      console:
        enabled: false
    sql:
      init:
        mode: never

mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
