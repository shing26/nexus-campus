server:
  port: 8081
  servlet:
    context-path: /
    encoding:
      charset: UTF-8
      enabled: true
      force: true
  compression:
    enabled: true
    mime-types: application/json,application/xml,text/html,text/plain,text/css,application/javascript
    min-response-size: 1024

spring:
  application:
    name: nexus-campus
  task:
    execution:
      pool:
        core-size: 4
        max-size: 10
        queue-capacity: 100
    thread-name-prefix: nexus-async-


  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.data.redis.RedisAutoConfiguration
      - org.springframework.boot.autoconfigure.data.redis.RedisRepositoriesAutoConfiguration

  sql:
    init:
      mode: always
      schema-locations: classpath:schema.sql
      data-locations: classpath:data.sql

  # JSP View Resolver
  mvc:
    view:
      prefix: /WEB-INF/views/
      suffix: .jsp

  # H2 Database (dev mode - switch to MySQL for production)
  datasource:
    url: jdbc:h2:mem:nexuscampus;DB_CLOSE_DELAY=-1;MODE=MYSQL;DATABASE_TO_LOWER=TRUE
    driver-class-name: org.h2.Driver
    username: sa
    password:

  h2:
    console:
      enabled: true
      path: /h2-console

  # Redis Configuration
  redis:
    enabled: true
    host: localhost
    port: 6379
    timeout: 3000
    lettuce:
      pool:
        max-active: 8
        max-idle: 8
        min-idle: 0

  # JPA / Hibernate
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true

  # File upload
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 20MB

# MyBatis-Plus
mybatis-plus:
  mapper-locations: classpath*:mapper/*.xml
  type-aliases-package: com.nexus.campus.entity
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    banner: false
    db-config:
      id-type: ASSIGN_ID
      logic-delete-field: deleted
      logic-delete-value: 1
      logic-not-delete-value: 0

# JWT Configuration
jwt:
  secret: OExHRjA3QjlGRjI0NUI0Q0E1RTdDRjI2MDA3QTUxMjhDOTdBNkY4RjI0NUI0Q0E1RTdDRjI2MDdCOTg2RjA3Qg==
  expiration: 86400000

# DFA Sensitive Words
campus:
  redis:
    enabled: false
  upload:
    dir: src/main/resources/static/uploads
  security:
    sensitive-words: fuck,shit,damn,asshole,bitch
    critical-words: 闁哄棙娼欐慨蹇涘礆閸℃凹妫?濡壆濮鹃々顐﹀炊閽樺鍟€,闂侇収浜濋弳鈧紓浣稿缁?闁诡厽鍔栭埀顒佺墧鐎靛本绋?闁哄鑳堕顒傗偓瑙勵殕閺嗏偓,闁告帒妫滈ˉ鍥╂嚊閳ь剛鎷?闁告瑥绉存慨鈺傜┍閳╁啩绱?
    audit-required-level: 2

# Logging
logging:
  level:
    com.nexus.campus: DEBUG
    org.springframework: INFO
    org.mybatis: INFO

---
management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: always

---
spring:
  config:
    activate:
      on-profile: mysql

  datasource:
    url: jdbc:mysql://localhost:3306/nexus_campus?useUnicode=true&characterEncoding=utf8mb4&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: root
    h2:
      console:
        enabled: false
    sql:
      init:
        mode: never

mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl